import { type IndexStruct } from "@llamaindex/core/data-structs";
import { BaseIndexStore } from "@llamaindex/core/storage/index-store";
import type { BaseKVStore } from "@llamaindex/core/storage/kv-store";
export declare class KVIndexStore extends BaseIndexStore {
    private _kvStore;
    private _collection;
    constructor(kvStore: BaseKVStore, namespace?: string);
    addIndexStruct(indexStruct: IndexStruct): Promise<void>;
    deleteIndexStruct(key: string): Promise<void>;
    getIndexStruct(structId?: string): Promise<IndexStruct | undefined>;
    getIndexStructs(): Promise<IndexStruct[]>;
}
